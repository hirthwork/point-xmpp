{% if aliases.user %}{{ _('User aliases') }}:
{% for a in aliases.user %}  {{a}} = {{aliases.user[a]}}
{% endfor %}{% endif %}
{% if aliases.global %}{{ _('Global aliases') }}:
{% for a in aliases.global %}{{a}} = {{aliases.global[a]}}
{% endfor %}{% endif %}
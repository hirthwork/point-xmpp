{% set _post = '#%s'|format(post_id) %}
{% for u in to_users %}@{{ u }} {% endfor %} {% if to_users|count > 1 %}{{ _('are removed from %s')|format(_post) }}{% else %}{{ _('is removed from %s')|format(_post) }}{% endif %}.